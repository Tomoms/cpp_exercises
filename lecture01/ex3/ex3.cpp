#include <iostream>

double evaluate_polynomial(const double x)
{
	const int a{1}, b{-6}, c{11}, d{6};
	const double result = a * (x*x*x) + b * (x*x) + c * x + d;
	return
}

int main()
{
	double lower{-10}, upper{10};
	double middle;
	while (upper - lower > 10e-9) {
		middle = (upper - lower) / 2;
		
	}

	return 0;
}
